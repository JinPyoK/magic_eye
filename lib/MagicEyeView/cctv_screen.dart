import 'package:flutter/material.dart';

class CCTVScreen extends StatelessWidget {
  const CCTVScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("CCTV Screen")),
    );
  }
}
